name: Build

on:
  push:
    branches:
      - master
      - production

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo GitHub Object
        run: |
          echo "${{ toJson(github) }}"
